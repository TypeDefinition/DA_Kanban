module.exports.isValidUsername = function isValidUsername(username) {
  if (!username) return false
  return true
}

module.exports.isValidPassword = function isValidPassword(password) {
  if (!password) return false
  return true
}

module.exports.isValidEmail = function isValidEmail(email) {
  if (!email) return false
  return true
}
